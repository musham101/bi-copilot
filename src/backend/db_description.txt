Available tables:
- Address: The Address table stores physical mailing addresses for entities like businesses and sales orders. It maintains a one-to-many relationship with the StateProvince table and is referenced by both BusinessEntityAddress and SalesOrderHeader tables to link entities to their respective addresses.
- AddressType: The AddressType table stores different categories of addresses, such as billing or shipping, and is referenced by the BusinessEntityAddress table to classify addresses associated with business entities. It serves as a lookup table for address types.
- AWBuildVersion: The `AWBuildVersion` table tracks different versions of a software build, storing the version number, associated date, and modification timestamp for each release. It likely serves to manage and track software deployment history, potentially relating to a broader system information table via the `SystemInformationID`.
- BillOfMaterials: The BillOfMaterials table defines the components required to assemble a product, specifying the quantity and unit of measure for each component at a particular level of the product hierarchy. It links product assemblies to components, referencing both the Product and UnitMeasure tables to provide detailed assembly information.
- BusinessEntity: The `BusinessEntity` table represents a core entity within the system, acting as a central link between people, stores, vendors, and their associated addresses and contact information. It serves as a unique identifier for each business entity, facilitating relationships with other tables that describe details about them.
- BusinessEntityAddress: The `BusinessEntityAddress` table links business entities (like people or organizations) to specific addresses and their types, such as billing or shipping. It acts as a linking table between `BusinessEntity`, `Address`, and `AddressType` to manage multiple addresses per entity.
- BusinessEntityContact: The `BusinessEntityContact` table stores contact information for individuals and businesses, linking a person or business entity to a specific contact type. It acts as a linking table between `BusinessEntity`, `Person`, and `ContactType` to represent how entities can have multiple contact methods.
- ContactType: The `ContactType` table defines the different categories of contact information, such as email, phone, or address, and is referenced by the `BusinessEntityContact` table to associate specific contact methods with business entities. It serves as a lookup table for contact types.
- CountryRegion: The `CountryRegion` table represents geographical regions, storing their codes and names. It serves as a foundational table, referenced by tables like `CountryRegionCurrency`, `SalesTerritory`, and `StateProvince` to define geographical context for related data.
- CountryRegionCurrency: The CountryRegionCurrency table establishes a many-to-many relationship between countries/regions and currencies, indicating which currencies are used within specific countries/regions. It tracks when these associations were last updated.
- CreditCard: The CreditCard table stores information about credit cards, including their type, number, and expiration date. It's linked to the PersonCreditCard table to associate cards with individuals and to the SalesOrderHeader table to track payments for sales orders.
- Culture: The Culture table stores information about distinct cultural settings or locales, identified by a unique ID and name. It's referenced by other tables to associate products and descriptions with specific cultural contexts.
- Currency: The `Currency` table stores information about different currencies, such as their code and name. It's referenced by tables tracking currency usage in countries and regions, as well as currency exchange rates.
- CurrencyRate: The `CurrencyRate` table stores historical exchange rates between different currencies, tracking the average and end-of-day rates for a given date. It links to the `Currency` table to define the currencies involved and is used by the `SalesOrderHeader` table to calculate order totals in a consistent currency.
- Customer: The `Customer` table stores information about individuals or organizations who are customers of the business, linking them to a person, store, and sales territory. It serves as a central point of reference for sales orders and related transactions.
- DatabaseLog: The `DatabaseLog` table stores records of database events, capturing details like the user, timestamp, event type, and associated SQL statements executed within the database. It serves as an audit trail, potentially linked to user accounts or database objects through the `DatabaseUser`, `Schema`, and `Object` fields.
- Department: The Department table represents organizational departments within a company, storing information like their name and the group they belong to. It's referenced by the EmployeeDepartmentHistory table, indicating which departments employees have been associated with over time.
- Document: The `Document` table stores information about digital documents, including their title, owner (linking to the `Employee` table), and version history. It serves as a central repository for document content and is referenced by the `ProductDocument` table, likely to associate documents with products.
- EmailAddress: The EmailAddress table stores email addresses associated with individuals, linking each address to a person via the BusinessEntityID. It tracks multiple email addresses per person, providing a way to manage contact information.
- Employee: The `Employee` table stores information about individuals employed by the organization, linking them to the `Person` table via a shared identifier and tracking details like job title, hire date, and compensation-related attributes. It is referenced by several other tables to track employment history, pay, and related activities.
- EmployeeDepartmentHistory: The EmployeeDepartmentHistory table tracks an employee's assignment to a specific department and shift over time. It records when an employee began and ended their association with a department and shift, referencing the Employee, Department, and Shift tables to provide context.
- EmployeePayHistory: The `EmployeePayHistory` table tracks changes to employee compensation over time, recording the rate and frequency of pay for each employee at specific dates. It links to the `Employee` table to identify the employee to whom the pay history applies.
- ErrorLog: The ErrorLog table records details about errors that occur within the system, capturing information like the user involved, error code, and specific location of the error. It serves as a centralized repository for troubleshooting and auditing purposes.
- Illustration: The `Illustration` table stores visual representations, likely images or diagrams, used to depict products or product models. It's referenced by the `ProductModelIllustration` table, suggesting a many-to-one relationship where multiple product models can be associated with a single illustration.
- JobCandidate: The JobCandidate table stores information about individuals who have applied for positions within the company, linking each candidate to a corresponding employee record who may be managing their application. It captures a candidate's resume and tracks when their information was last updated.
- Location: The `Location` table represents physical locations within a system, storing information like name and cost rate. It's referenced by `ProductInventory` and `WorkOrderRouting` to indicate where products are stored or work orders are processed.
- Password: The Password table stores the salted and hashed password information for individual users represented in the Person table, linking each password to a specific person via the BusinessEntityID. It ensures secure authentication by storing password credentials in a protected format.
- Person: The `Person` table stores information about individuals within the system, representing people who may be customers, employees, or otherwise interact with the business. It links to the `BusinessEntity` table and is referenced by several other tables to represent various contact details, credentials, and financial information associated with each person.
- PersonCreditCard: The PersonCreditCard table tracks which credit cards are associated with which individuals, establishing a many-to-many relationship between people and their credit cards. It records when these associations were modified.
- PersonPhone: The PersonPhone table stores contact information, specifically associating individuals (referenced from the Person table) with their phone numbers and the type of phone number (e.g., mobile, work) as defined in the PhoneNumberType table. It acts as a linking table to manage multiple phone numbers per person.
- PhoneNumberType: The `PhoneNumberType` table stores different categories of phone numbers, such as "Home," "Mobile," or "Work," and is referenced by the `PersonPhone` table to classify phone numbers associated with individuals. It serves to standardize and categorize phone number types within the database.
- Product: The `Product` table stores information about individual products offered by the business, including details like name, cost, pricing, manufacturing time, and related attributes. It establishes relationships with product models, subcategories, and unit measures, and is used by numerous other tables to track inventory, sales, and vendor information.
- ProductCategory: The ProductCategory table stores information about the broad categories into which products are organized, such as "Electronics" or "Clothing." It serves as a parent category for more specific product subcategories.
- ProductCostHistory: The `ProductCostHistory` table tracks changes to the standard cost of products over time, recording the cost in effect between specific start and end dates. It maintains a historical record of product costs and relates to the `Product` table via the `ProductID`.
- ProductDescription: The `ProductDescription` table stores detailed descriptions of products, associating them with a unique identifier. It's referenced by other tables to link descriptions to specific product models and language cultures.
- ProductDocument: The ProductDocument table stores versions of documents associated with specific products, linking product records to document records and tracking when those documents were modified. It serves as a history table, allowing for multiple versions of a document to be associated with a single product.
- ProductInventory: The `ProductInventory` table tracks the quantity of specific products available at particular locations within a warehouse or store. It represents the intersection of products and locations, linking them together and detailing the inventory level at each product's storage spot.
- ProductListPriceHistory: The `ProductListPriceHistory` table tracks the changes in list prices for products over time. It stores the list price for a specific product between a start and end date, referencing the `Product` table to identify the product.
- ProductModel: The `ProductModel` table represents a specific model of a product, storing its name, description, and instructions. It's referenced by tables detailing product instances, illustrations, and localized descriptions, indicating that multiple products can share the same model.
- ProductModelIllustration: The ProductModelIllustration table tracks which illustrations are associated with specific product models, essentially linking visual representations to product designs. It serves as a many-to-many relationship between ProductModel and Illustration entities.
- ProductModelProductDescriptionCulture: The `ProductModelProductDescriptionCulture` table stores localized descriptions for product models, linking a specific product model and description to a particular culture or language. It acts as a bridge between product models, their descriptions, and the cultures they apply to, enabling multilingual product information.
- ProductPhoto: The `ProductPhoto` table stores image data associated with products, holding both thumbnail and large versions of product photos. It's linked to the `ProductProductPhoto` table, indicating which products are associated with which photos.
- ProductProductPhoto: The `ProductProductPhoto` table represents the relationship between products and their associated photos, indicating which photos belong to which products. It acts as a linking table, allowing a product to have multiple photos and a photo to be associated with multiple products.
- ProductReview: The `ProductReview` table stores customer feedback and ratings for products. Each record represents a review submitted by a user, linked to a specific product via a foreign key relationship.
- ProductSubcategory: The `ProductSubcategory` table represents a hierarchical categorization of products, detailing subcategories within broader product categories. It establishes a parent-child relationship with the `ProductCategory` table and is used to classify individual products.
- ProductVendor: The ProductVendor table tracks the relationship between products and vendors, detailing key supply chain information like pricing, lead times, and order quantities. It links products to vendors and utilizes unit measure codes, enabling the management of product procurement and vendor performance.
- PurchaseOrderDetail: The `PurchaseOrderDetail` table tracks the individual items and their details within a purchase order, linking to both a purchase order header and a product record. It stores information like quantity ordered, due date, and received quantities for each product included in a purchase order.
- PurchaseOrderHeader: The `PurchaseOrderHeader` table tracks the header information for purchase orders created by employees to request goods or services from vendors, including order details like dates, shipping method, and total amounts. It maintains a one-to-many relationship with the `PurchaseOrderDetail` table to store individual items within each order and references `Employee`, `Vendor`, and `ShipMethod` tables for related information.
- SalesOrderDetail: The SalesOrderDetail table represents individual items included within a sales order, detailing the quantity, price, and discounts applied to each product. It links sales orders to specific products and associated special offers, capturing the specifics of each line item.
- SalesOrderHeader: The `SalesOrderHeader` table stores information about sales orders placed by customers, including order details like dates, shipping addresses, and payment information. It maintains relationships with tables representing customers, salespersons, addresses, and shipping methods to capture the context of each order.
- SalesOrderHeaderSalesReason: The SalesOrderHeaderSalesReason table tracks the specific sales reasons associated with each sales order, linking sales orders to reasons for the sale. It serves as a linking table between SalesOrderHeader and SalesReason tables, enabling analysis of why particular orders were placed.
- SalesPerson: The SalesPerson table stores information about individual sales representatives, including their sales performance metrics and assigned sales territory. It links employees to sales territories and is used to track sales order history and quota performance.
- SalesPersonQuotaHistory: The SalesPersonQuotaHistory table tracks the sales quota assigned to each salesperson over time. It records the quota amount for a specific date, referencing the SalesPerson table to identify the salesperson to whom the quota applies.
- SalesReason: The `SalesReason` table stores a catalog of reasons used to explain sales transactions, categorizing them by type. It's referenced by the `SalesOrderHeaderSalesReason` table, indicating which reasons apply to specific sales orders.
- SalesTaxRate: The SalesTaxRate table stores details about sales tax rates applicable to a specific state or province. It links to the StateProvince table to define the geographic region to which each tax rate applies.
- SalesTerritory: The `SalesTerritory` table represents geographic regions used for organizing sales activities, storing information about each territory including its name, country, and sales performance metrics. It maintains a relationship with the `CountryRegion` table and is used by several other tables to associate customers, sales orders, and sales personnel with specific territories.
- SalesTerritoryHistory: The SalesTerritoryHistory table tracks the assignment of salespersons to specific sales territories over time. It records which salesperson was responsible for a territory during a defined period, referencing both the salesperson and the sales territory.
- ScrapReason: The `ScrapReason` table stores a list of reasons why materials are scrapped, referencing these reasons from work orders to track material loss and identify potential issues. Each reason is uniquely identified by a `ScrapReasonID` and includes a descriptive `Name`.
- Shift: The `Shift` table defines recurring work shifts, specifying their name, start and end times, and tracking modification history. It's referenced by `EmployeeDepartmentHistory` to associate employees with specific shifts during their employment.
- ShipMethod: The `ShipMethod` table stores information about different shipping options available, defining their base costs and rates. It's referenced by both `PurchaseOrderHeader` and `SalesOrderHeader` tables to indicate the chosen shipping method for orders.
- ShoppingCartItem: The ShoppingCartItem table tracks individual products added to a customer's shopping cart, associating a quantity with each product within a specific cart. It establishes a relationship with the Product table to identify the items being tracked.
- SpecialOffer: The `SpecialOffer` table stores details about promotional offers, including their description, discount percentage, validity period (start and end dates), and quantity restrictions. It is linked to the `SpecialOfferProduct` table, indicating which products are associated with each offer.
- SpecialOfferProduct: The SpecialOfferProduct table represents the association between specific products and special offers, indicating which products are included in a particular promotion. It acts as a linking table between the Product and SpecialOffer tables, and is used to track which products are part of a given special offer, ultimately influencing sales order details.
- StateProvince: The `StateProvince` table represents geographic subdivisions within countries, storing information about states or provinces and their associated country and sales territory. It maintains relationships with both `CountryRegion` to identify the country and `SalesTerritory` to define the sales region it belongs to, and is referenced by tables like `Address` and `SalesTaxRate`.
- Store: The `Store` table represents retail store locations within the business, associating each store with a salesperson and demographic information. It maintains a relationship with the `SalesPerson` table to track assigned sales representatives and is referenced by the `Customer` table, likely to indicate where customers shop.
- TransactionHistory: The TransactionHistory table tracks individual transactions related to products, recording details like quantity, cost, and type. Each transaction is linked to a product and may be associated with a specific order and line item within that order.
- TransactionHistoryArchive: The TransactionHistoryArchive table stores historical records of product transactions, linking them to specific orders and order lines. It serves as an immutable log of transactions, capturing details like quantity, cost, and type over time.
- UnitMeasure: The UnitMeasure table stores information about units of measurement, such as "each," "pound," or "gallon," and is referenced by tables tracking product components, product details, and vendor information. It serves as a lookup table to standardize measurement units across the database.
- Vendor: The Vendor table stores information about entities that supply goods or services, linking them to the broader BusinessEntity table and serving as a supplier for products and purchase orders. It tracks key details like account numbers, credit ratings, and vendor status, establishing relationships with products and purchase order headers.
- WorkOrder: The WorkOrder table tracks the production of goods, detailing the quantity ordered, the amount stocked, and any scrapped items for a specific product. It references product and scrap reason information and is used by the WorkOrderRouting table to define the production steps involved.
- WorkOrderRouting: The `WorkOrderRouting` table tracks the sequence of operations required to produce a product within a work order, detailing the planned and actual schedule, resource usage, and costs associated with each step. It links work orders to products and locations, representing a specific routing step within the overall manufacturing process.